name: fasttext
description: Given an text, detect the language of it.
port: 5014
weights:
  - fasttext/lid.176.bin
inputs:
  - endpoint: /put/text
    queue: zero
    required:
      text: text
daemons:
  - api
  - ai/query
